import React from 'react';
import { renderWithClient } from '@tests/utils'; // TODO: change to your lib to render test screens

import <%= componentName %> from '.';

describe('<%= componentName %>', () => {
  it('should render', () => {
    renderWithClient(<<%= componentName %> />);
  });

  // TODO: ADD MORE CASES OF TESTS
});
